# 7장: 분산 시스템을 위한 유일 ID 생성기 설계

분산 환경에서 유일한 ID를 만들어야 한다면, 단순히 관계형 DB의 `auto_increment` 기본 키를 쓰면 되지 않을까 생각하기 쉽다. 하지만 분산 환경에서는 이 방식이 통하지 않는다. DB 서버 한 대로는 그 규모를 감당할 수 없고, 여러 DB 서버를 쓰면 지연(delay)을 낮추기가 매우 어렵기 때문이다.

유일성이 보장되는 ID 예시:
```
user_id
1227238262110117894
1241107244890099715
1243643959492173824
1247686501489692673
1567981766075453440
```

---

## 1단계: 문제 이해 및 설계 범위 확정

면접에서는 적절한 질문으로 모호함을 없애고 설계 방향을 정하는 게 첫 단계다.

면접관–지원자 대화 예시:
- **ID는 어떤 특성을 갖나?** → 유일해야 하고, 정렬 가능해야 한다.
- **새 레코드의 ID는 항상 1만큼 큰 값인가?** → 시간 흐름에 따라 커지기는 하지만 항상 +1은 아니다. 다만 저녁에 만든 ID가 아침에 만든 ID보다 크다는 점은 확실하다.
- **ID는 숫자로만 구성되나?** → 그렇다.
- **시스템 규모는?** → 초당 10,000개의 ID를 생성할 수 있어야 한다.

### 요구사항 정리
- ID는 **유일**해야 한다.
- ID는 **숫자로만** 구성되어야 한다.
- ID는 **64비트**로 표현될 수 있어야 한다.
- ID는 **발급 날짜순으로 정렬** 가능해야 한다.
- 초당 **10,000개**의 ID를 만들 수 있어야 한다.

---

## 2단계: 개략적 설계안 제시 및 동의 구하기

분산 시스템에서 유일성이 보장되는 ID를 만드는 방법은 여러 가지다. 다음 네 가지 선택지를 살펴본다.

- 다중 마스터 복제 (multi-master replication)
- UUID (Universally Unique Identifier)
- 티켓 서버 (ticket server)
- 트위터 스노플레이크 (twitter snowflake)

---

### 방법 1. 다중 마스터 복제 (Multi-master Replication)

DB의 `auto_increment` 기능을 활용하되, 다음 ID 값을 구할 때 **1이 아니라 k(=DB 서버 수)만큼 증가**시킨다.

```
MySQL #1 ──→  1, 3, 5, ...  ┐
                            ├──→  웹 서버
MySQL #2 ──→  2, 4, 6, ...  ┘
```

서버가 만들 다음 ID는 그 서버가 생성한 이전 ID에 전체 서버 수(k)를 더한 값이다. DB 수를 늘리면 초당 생성 가능 ID 수도 늘어난다.

**단점**
- 여러 데이터 센터에 걸쳐 규모를 늘리기 어렵다.
- ID의 유일성은 보장되지만, 시간 흐름에 맞춰 커지도록 보장할 수는 없다.
- 서버를 추가/삭제할 때 잘 동작하도록 만들기 어렵다.

---

### 방법 2. UUID

컴퓨터 시스템에 저장되는 정보를 유일하게 식별하기 위한 **128비트짜리 수**. 중복 가능성이 지극히 낮다. (위키피디아: 중복 UUID 1개 생길 확률을 50%로 끌어올리려면 초당 10억 개를 100년 동안 만들어야 함)

UUID 값 형태 예시:
```
09c93e62-50b4-468d-bf8a-c07e1040bfb2
```

서버 간 **조율 없이 독립적으로 생성** 가능하다.

```
웹 서버      웹 서버      웹 서버      웹 서버
[ID 생성기]  [ID 생성기]  [ID 생성기]  [ID 생성기]
   ↑ 각 서버가 별도의 ID 생성기로 독립적으로 ID 생성
```

**장점**
- 만들기 단순하다. 서버 간 조율이 필요 없어 동기화 이슈가 없다.
- 각 서버가 자기가 쓸 ID를 알아서 만드는 구조라 규모 확장도 쉽다.

**단점**
- ID가 **128비트로 길다** (이번 문제 요구사항은 64비트).
- ID를 **시간순으로 정렬할 수 없다**.
- ID에 숫자(numeric) 아닌 값이 포함될 수 있다.

---

### 방법 3. 티켓 서버 (Ticket Server)

`auto_increment` 기능을 갖춘 **DB 서버 한 대를 중앙 집중형 티켓 서버**로 사용한다. (Flickr가 분산 기본 키 생성에 이 기술을 사용)

```
웹 서버   웹 서버   웹 서버   웹 서버
   └────────┴───┬────┴────────┘
               ▼
           티켓 서버
       (auto_increment DB)
```

**장점**
- 유일성이 보장되는 오직 숫자로만 구성된 ID를 쉽게 만들 수 있다.
- 구현이 쉽고, 중소 규모 애플리케이션에 적합하다.

**단점**
- 티켓 서버가 **SPOF(Single Point of Failure)** 가 된다. 이를 피하려고 티켓 서버를 여러 대 두면 데이터 동기화 같은 새로운 문제가 생긴다.

---

### 방법 4. 트위터 스노플레이크 (Snowflake) 접근법

ID를 바로 생성하는 대신 **분할 정복(divide and conquer)** 을 적용해, ID를 여러 절(section)로 나눈다. 이번 문제의 요구사항(유일성, 64비트, 시간순 정렬, 초당 1만 개)을 모두 만족시킬 수 있다.

#### 64비트 ID 구조
```
┌─1비트─┬──── 41비트 ────┬─5비트─┬─5비트─┬── 12비트 ──┐
│ 0     │   타임스탬프     │ DC ID │서버 ID│  일련번호   │
└───────┴────────────────┴───────┴───────┴────────────┘
```

각 절의 쓰임새:
- **사인(sign) 비트 (1비트)**: 지금은 안 쓰지만 음수/양수 구분을 위해 유보. 항상 0.
- **타임스탬프 (41비트)**: 기원 시각(epoch) 이후 몇 밀리초가 지났는지. 가장 중요한 절. 스노플레이크 기본 epoch는 `1288834974657` (Nov 04, 2010, 01:42:54 UTC).
- **데이터센터 ID (5비트)**: 2⁵ = 32개 데이터센터 지원.
- **서버 ID (5비트)**: 데이터센터당 32개 서버 사용.
- **일련번호 (12비트)**: 서버가 ID를 생성할 때마다 1씩 증가, 1밀리초가 지날 때마다 0으로 초기화(reset).

---

## 3단계: 상세 설계

스노플레이크 접근법으로 상세 설계를 진행한다.

```
┌─1비트─┬──── 41비트 ────┬─5비트─┬─5비트─┬── 12비트 ──┐
│ 0     │   타임스탬프     │ DC ID │서버 ID│  일련번호   │
└───────┴────────────────┴───────┴───────┴────────────┘
```

### 데이터센터 ID / 서버 ID
- 시스템이 **시작할 때 결정**되며, 운영 중에는 보통 바뀌지 않는다.
- 잘못 변경하면 ID 충돌이 발생할 수 있으므로 변경 작업은 신중해야 한다.

### 타임스탬프 (41비트)
가장 중요한 절. 시간이 흐를수록 큰 값을 가지므로 ID를 **시간순으로 정렬** 가능하게 만든다.

비트 표현 → UTC 변환 예시:
```
이진 타임스탬프 값
   │  (십진 변환)
   ▼
297616116568
   │  (트위터 기원 시각 epoch 더함)
   ▼
1586451091225
   │  (밀리초 → UTC 변환)
   ▼
Apr 09 2020 16:51:31 UTC
```

41비트로 표현 가능한 최댓값은 `2⁴¹ − 1 = 2199023255551` 밀리초 ≈ **약 69년**.
- 따라서 이 ID 생성기는 69년 동안만 정상 동작한다.
- 기원 시각을 현재에 가깝게 맞춰 오버플로(overflow) 시점을 늦춘 것.
- 69년이 지나면 기원 시각을 바꾸거나 ID 체계를 다른 것으로 이전(migration)해야 한다.

### 일련번호 (12비트)
- `2¹² = 4096` 개의 값을 가질 수 있다.
- 같은 밀리초 동안 하나 이상의 ID를 만들어 낸 경우에만 0보다 큰 값을 갖는다.

---

## 4단계: 마무리

네 가지 방법(다중 마스터 복제, UUID, 티켓 서버, 트위터 스노플레이크)을 살펴봤고, 최종적으로 **스노플레이크**를 선택했다. 모든 요구사항을 만족하면서 분산 환경에서 규모 확장이 가능했기 때문이다.

### 추가 논의 거리
- **시계 동기화 (clock synchronization)**: 설계에서 모든 ID 생성 서버가 같은 시계를 쓴다고 가정했지만, 한 서버가 여러 코어에서 실행되거나 물리적으로 독립된 여러 장비에서 실행될 경우 이 가정은 깨질 수 있다. 해결책으로 **NTP(Network Time Protocol)** 가 가장 보편적이다.
- **각 절(section)의 길이 최적화**: 예를 들어 동시성(concurrency)이 낮고 수명이 긴 애플리케이션이라면, 일련번호 절을 줄이고 타임스탬프 절을 늘리는 것이 효과적일 수 있다.

---

## 방법별 비교 요약

| 방법 | 장점 | 단점 |
|------|------|------|
| 다중 마스터 복제 | DB 기능 재사용, 숫자 ID | 데이터센터 확장·시간순 정렬·서버 증감 어려움 |
| UUID | 단순, 조율 불필요, 확장 쉬움 | 128비트로 길다, 정렬 불가, 비숫자 포함 가능 |
| 티켓 서버 | 숫자 ID, 구현 쉬움, 중소규모 적합 | SPOF, 다중화 시 동기화 문제 |
| 스노플레이크 | 유일성·64비트·정렬·확장 모두 만족 | 시계 동기화 필요, 69년 수명 제한 |

---

## 질문

**Q1.** 분산 환경에서 관계형 DB의 `auto_increment`를 그대로 쓰면 안 되는 이유는 무엇인가?

**Q2.** UUID는 서버 간 조율 없이 ID를 만들 수 있어 편리한데, 왜 이번 문제(64비트, 시간순 정렬)에는 부적합한가?

**Q3.** 스노플레이크의 64비트 구조에서 각 절(타임스탬프/DC ID/서버 ID/일련번호)은 어떤 역할을 하며, 왜 타임스탬프가 가장 중요한가?

**Q4.** 스노플레이크 ID 생성기가 약 69년만 동작하는 이유는 무엇이며, 69년이 지나면 어떻게 대응해야 하는가?