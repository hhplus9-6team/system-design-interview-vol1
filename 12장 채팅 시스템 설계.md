# 12장 채팅 시스템 설계

> **"실시간으로 수천만 명이 사용하는 채팅 서비스는 어떻게 설계할까?"**

채팅 시스템은 단순히 메시지를 저장하는 서비스가 아니다.

낮은 지연시간(Low Latency), 항상 유지되는 연결(Connection), 여러 단말 간 메시지 동기화, 접속 상태 관리, 푸시 알림까지 모두 고려해야 하는 대표적인 **실시간 시스템(Realtime System)** 이다.

이번 장에서는 **DAU 5천만 명 규모의 채팅 시스템**을 설계하는 과정을 단계별로 살펴본다.

---

## 1. 문제 이해 및 설계 범위 확정

시스템 설계에서 가장 먼저 해야 할 일은 **요구사항을 명확하게 정의하는 것**이다.

채팅 서비스는 지원해야 하는 기능에 따라 전체 아키텍처가 크게 달라질 수 있기 때문이다.

### 요구사항

| 항목        | 내용             |
| --------- | -------------- |
| 채팅 방식     | 1:1 채팅 + 그룹 채팅 |
| 플랫폼       | 웹, 모바일         |
| DAU       | 5천만 명          |
| 그룹 채팅 인원  | 최대 100명        |
| 메시지 종류    | 텍스트            |
| 메시지 길이    | 100,000자 이하    |
| 접속 상태 표시  | 지원             |
| 다중 단말 로그인 | 지원             |
| 푸시 알림     | 지원             |
| 채팅 보관 기간  | 영구 보관          |
| 종단 간 암호화  | 향후 고려          |


### 핵심 기능

이번 설계에서 구현해야 하는 핵심 기능은 다음과 같다.

* 응답 지연이 낮은 1:1 채팅
* 최대 100명의 그룹 채팅
* 사용자 접속 상태 표시
* 여러 단말에서 동시에 로그인 지원
* 푸시 알림 지원

---

## 2. 개략적 설계안

채팅 시스템의 핵심 기능은 크게 다음과 같다.

* 클라이언트로부터 메시지 수신
* 메시지 저장
* 수신자에게 전달
* 오프라인 사용자의 메시지 보관
* 푸시 알림 전송


### 어떤 통신 프로토콜을 사용할 것인가?

채팅 서비스는 **실시간 통신**이 핵심이다.

후보가 되는 방식은 크게 세 가지이다.

#### 1) Polling

가장 단순한 방식이다.

클라이언트가 일정 주기마다

> "새로운 메시지가 있나요?"

를 반복적으로 요청한다.

```
Client -----> Server
Client -----> Server
Client -----> Server
```

##### 장점

* 구현이 가장 쉽다.

##### 단점

* 대부분의 요청이 빈 응답이다.
* 불필요한 네트워크 트래픽이 발생한다.
* 실시간성이 떨어진다.

#### 2) Long Polling

Polling의 비효율을 줄이기 위한 방식이다.

새로운 메시지가 올 때까지 서버가 요청을 유지한다.

```
Client -------------->
          (대기)

메시지 발생

Server -------------->
```

##### 장점

* Polling보다 네트워크 낭비가 적다.

##### 단점

* 메시지를 많이 받지 않는 사용자도 계속 재연결해야 한다.
* 클라이언트 연결 종료 여부를 서버가 즉시 알기 어렵다.
* 송신자와 수신자가 서로 다른 채팅 서버에 연결될 경우 처리 구조가 복잡해진다.


#### 3) WebSocket

현재 대부분의 실시간 채팅 서비스에서 사용하는 방식이다.

초기 연결은 HTTP로 시작하지만 Handshake 이후 WebSocket으로 업그레이드된다.

이후에는 **하나의 TCP 연결을 계속 유지하며 양방향 통신**이 가능하다.

```
HTTP Handshake

↓

WebSocket Connection

↓

Client  <------->  Server
```

##### 장점

* 서버가 원하는 시점에 메시지를 Push할 수 있다.
* 연결을 계속 유지하므로 응답 지연이 매우 낮다.
* 실시간 서비스에 적합하다.

> 따라서 대규모 채팅 시스템에서는 WebSocket을 사용하는 것이 일반적이다.


### 전체 시스템 구성

채팅 시스템은 크게 네 가지 영역으로 구성된다.

#### 1) 무상태 서비스 (Stateless Service)

로그인, 회원가입, 사용자 정보 조회 등 일반적인 REST API를 담당한다.

* 로그인
* 회원가입
* 사용자 프로필 조회

로드밸런서 뒤에서 여러 서버가 동일하게 처리할 수 있다.


#### 2) 상태 유지 서비스 (Stateful Service)

채팅 서버는 사용자와 WebSocket 연결을 계속 유지해야 한다.

따라서 연결 상태를 관리하는 **유일한 Stateful 서비스**이다.

```
사용자 A

↓

WebSocket

↓

Chat Server
```

#### 3) 푸시 알림 서버

사용자가 접속 중이 아니라면

메시지를 즉시 받을 수 없기 때문에

APNs 또는 FCM 같은 Push 서비스를 이용해 알림을 보낸다.


#### 4) 확장성

예를 들어

동시 접속자가 100만 명이고

사용자당 WebSocket 연결 하나가 약 **10KB 메모리**를 사용한다고 가정하면

```
1,000,000 × 10KB

≈ 10GB
```

즉, 메모리만 충분하다면 하나의 서버에서도 상당한 수의 연결을 유지할 수 있으며, 이후 트래픽 증가에 따라 채팅 서버를 수평 확장하면 된다.


### 저장소 설계

데이터의 특성에 따라 저장소를 구분한다.

#### 관계형 데이터베이스

일반적인 서비스 데이터는 관계형 데이터베이스가 적합하다.

예를 들어

* 사용자 정보
* 친구 목록
* 설정

등은 안정성과 트랜잭션이 중요하기 때문이다.


#### 채팅 이력

채팅 메시지는 대부분

* 쓰기 빈도가 높고
* 최근 메시지를 자주 조회한다.

이러한 특성 때문에 **Key-Value 저장소**가 적합하다.

##### 이유

* 매우 낮은 조회 지연시간
* 대량 데이터 저장에 유리
* 수평 확장이 쉽다.

반면 관계형 데이터베이스는 데이터가 증가하면서 인덱스 크기가 커지고 Random Access 비용이 증가한다.

실제로 많은 메신저 서비스가 채팅 이력 저장소로 Key-Value 기반 데이터베이스를 사용한다.


### 데이터 모델

#### 1:1 채팅

| 컬럼           | 설명     |
| ------------ | ------ |
| message_id   | 메시지 ID |
| message_from | 발신자    |
| message_to   | 수신자    |
| content      | 내용     |
| created_at   | 생성 시간  |


#### 그룹 채팅

| 컬럼           | 설명     |
| ------------ | ------ |
| channel_id   | 채널 ID  |
| message_id   | 메시지 ID |
| message_from | 발신자    |
| content      | 내용     |
| created_at   | 생성 시간  |


#### 메시지 ID 설계

메시지 ID는 다음 조건을 만족해야 한다.

* 전역적으로 유일해야 한다.
* 시간 순서대로 정렬 가능해야 한다.
* 빠르게 생성되어야 한다.

관계형 데이터베이스에서는 Auto Increment를 사용할 수 있지만

NoSQL에서는 일반적으로 사용할 수 없다.

따라서

* Snowflake와 같은 전역 ID 생성기
* 지역(Local) 순서 번호 생성기

등을 사용할 수 있다.

특히 그룹 채팅에서는 그룹 내부에서만 순서가 보장되어도 충분하기 때문에 지역 순서 생성기를 사용하는 것도 좋은 선택이다.

---

## 3. 상세 설계

### 서비스 탐색(Service Discovery)

사용자가 접속하면

가장 적절한 채팅 서버를 찾아주는 역할을 수행한다.

대표적인 오픈소스로는

* Apache ZooKeeper

등이 사용된다.


### 1:1 채팅 메시지 흐름

메시지 전달 과정은 다음과 같다.

```
① 사용자 A

↓

② Chat Server 1

↓

③ Message ID 생성

↓

④ 메시지 동기화 큐

↓

⑤ Key-Value 저장소 저장

↓

⑥ 사용자 B 접속 여부 확인

 ├─ Online
 │
 │   Chat Server 2
 │
 │        ↓
 │
 │     사용자 B
 │
 └─ Offline
      ↓
 Push Server
```

접속 중인 사용자는 WebSocket으로 즉시 전달되고,

오프라인 사용자는 Push 알림을 받는다.


### 여러 단말 동기화

하나의 계정을

* 스마트폰
* 태블릿
* PC

에서 동시에 사용할 수도 있다.

각 단말은

```
cur_max_message_id
```

라는 값을 유지한다.

새로운 메시지를 조회할 때는

현재 단말이 마지막으로 받은 메시지 ID 이후의 메시지만 가져오면 된다.

이 방식은 여러 기기 간 메시지 동기화를 단순하게 만들어 준다.


### 그룹 채팅 메시지 처리

소규모 그룹에서는

사용자별 메시지 큐를 따로 유지하는 방식이 효율적이다.

```
Message

↓

User A Queue

User B Queue

User C Queue
```

사용자는 자신의 큐만 확인하면 되므로 동기화 구조가 단순하다.

다만 수천 명이 참여하는 대규모 그룹이라면

같은 메시지를 모든 사용자 큐에 복사하는 비용이 매우 커지므로 다른 전략이 필요하다.


### 접속 상태 관리

접속 상태 서버는 사용자의 Online / Offline 상태를 관리한다.

#### 1) 로그인

WebSocket 연결이 완료되면

```
status = online
last_active_at = 현재 시간
```

을 Key-Value 저장소에 저장한다.


#### 2) 로그아웃

사용자가 로그아웃하면

```
online

↓

offline
```

으로 변경한다.


#### 3) 비정상 종료

네트워크가 끊기거나 앱이 강제 종료될 수도 있다.

이 경우를 위해 Heartbeat를 사용한다.

클라이언트는 일정 주기마다

```
Heartbeat
```

를 서버로 전송한다.

정해진 시간 동안 Heartbeat가 도착하지 않으면

접속이 끊어진 것으로 판단하여 Offline으로 변경한다.


#### 4) 상태 변경 알림

친구의 접속 상태가 변경되면

발행-구독(Publish-Subscribe) 모델을 이용해 친구들에게 상태 변경 이벤트를 전달한다.

이 방식은 친구 수가 많지 않은 일반적인 메신저 환경에서 효율적으로 동작한다.



## 4. 마무리

이번 장에서는 **대규모 채팅 시스템의 전체 구조**를 살펴보았다.

핵심 내용은 다음과 같다.

* WebSocket 기반의 실시간 양방향 통신
* Stateful 채팅 서버와 Stateless API 서버의 분리
* Key-Value 저장소를 이용한 채팅 이력 저장
* 메시지 ID 생성 전략
* 사용자별 메시지 동기화
* 여러 단말 간 메시지 동기화
* Heartbeat 기반 접속 상태 관리
* Push 알림을 통한 오프라인 사용자 지원

추가로 실제 서비스에서는 다음과 같은 기능도 함께 고려할 수 있다.

* 사진·동영상 전송
* 종단 간 암호화(End-to-End Encryption)
* 캐시 적용
* 메시지 로딩 최적화
* 메시지 재전송 및 장애 복구 전략

---

### 같이 생각해 볼 질문

1. 왜 채팅 서버만 Stateful이고 로그인 서버는 Stateless일까?

2. 채팅 이력을 관계형 데이터베이스 대신 Key-Value 저장소에 저장하는 이유는 무엇일까?

3. WebSocket 연결 수가 수천만 개까지 증가한다면 어떤 방식으로 서버를 확장할 수 있을까?

4. 그룹 채팅 인원이 100명이 아니라 10만 명이라면 현재 구조는 그대로 사용할 수 있을까?

5. 메시지 순서를 보장하기 위해 Snowflake와 같은 전역 ID 생성기를 사용하는 것과 채널별(Local) 순서 생성기를 사용하는 것의 장단점은 무엇일까?
