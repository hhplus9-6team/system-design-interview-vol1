# 13장 검색어 자동완성 시스템

## 1. 개요

검색어 자동완성 시스템은 사용자가 검색창에 일부 문자를 입력했을 때, 입력한 문자열로 시작하는 인기 검색어를 빠르게 추천하는 시스템이다.

예를 들어 사용자가 `트위`를 입력하면 다음과 같은 검색어를 보여줄 수 있다.

```text
트위터
트위터 로그인
트위터 검색
```

검색어 자동완성 시스템을 설계할 때는 다음 두 가지가 중요하다.

1. 사용자가 입력한 접두어와 일치하는 검색어를 빠르게 찾는 것
2. 일치하는 검색어 중 가장 인기 있는 검색어를 우선적으로 보여주는 것

---

## 2. 요구사항

책에서는 면접관과의 질의응답을 통해 다음과 같은 요구사항을 정한다.

* 자동완성 결과는 사용자가 입력한 검색어의 접두어와 일치해야 한다.
* 검색어는 인기도 등의 순위에 따라 정렬되어야 한다.
* 자동완성 결과는 빠르게 반환되어야 한다.
* 대소문자를 구분하지 않는다.
* 특수문자는 처리하지 않는다고 가정한다.
* 자동완성 결과는 일정 개수만 반환한다.
* 검색어 순위는 실시간으로 갱신될 필요는 없으며, 일정한 주기로 갱신할 수 있다.

검색어를 입력할 때마다 요청이 발생하므로, 응답 속도가 매우 중요하다.

---

## 3. 개략적인 규모 추정

사용자가 검색창에 단어 하나를 입력하더라도 각 문자를 입력할 때마다 자동완성 요청이 발생한다.

예를 들어 사용자가 `hello`를 입력하면 다음과 같이 여러 번 요청될 수 있다.

```text
h
he
hel
hell
hello
```

따라서 실제 검색 횟수보다 자동완성 API의 요청 수가 훨씬 많을 수 있다.

시스템은 대량의 읽기 요청을 처리해야 하며, 새로운 검색어를 저장하고 검색 횟수를 집계하는 쓰기 작업도 처리해야 한다.

검색어 자동완성 시스템은 일반적으로 쓰기보다 읽기가 훨씬 많은 시스템이다.

---

## 4. 데이터 수집 서비스

자동완성 결과를 만들기 위해서는 사용자가 실제로 검색한 검색어와 검색 횟수를 수집해야 한다.

사용자가 검색하면 검색어와 검색 횟수를 저장한다.

```text
검색어        검색 횟수
--------------------
twitter       35
twitch        29
tiktok        20
```

가장 단순한 방법은 사용자가 검색할 때마다 데이터베이스의 검색 횟수를 증가시키는 것이다.

```sql
UPDATE query_frequency
SET frequency = frequency + 1
WHERE query = 'twitter';
```

하지만 검색 요청이 많을 경우 매 요청마다 데이터베이스를 갱신하면 쓰기 부하가 커진다.

따라서 검색 로그를 먼저 수집한 뒤, 일정한 주기로 검색 횟수를 집계하는 방식을 사용할 수 있다.

---

## 5. 데이터 분석 서비스

데이터 분석 서비스는 수집된 검색 로그를 분석해 검색어별 사용 빈도를 계산한다.

예를 들어 검색 로그가 다음과 같다고 하자.

```text
twitter
twitter
twitch
twitter
tiktok
```

집계 결과는 다음과 같다.

```text
twitter: 3
twitch: 1
tiktok: 1
```

이렇게 계산된 검색 빈도는 자동완성 결과의 순위를 정할 때 사용된다.

검색어 순위는 반드시 실시간으로 갱신할 필요는 없다. 검색 빈도를 일정 시간 단위로 집계한 뒤 자동완성 데이터를 갱신할 수 있다.

---

## 6. 관계형 데이터베이스를 이용한 초기 설계

검색어와 사용 빈도를 다음과 같은 테이블에 저장할 수 있다.

| query   | frequency |
| ------- | --------: |
| twitter |        35 |
| twitch  |        29 |
| tiktok  |        20 |

사용자가 `tw`를 입력하면 다음과 같은 쿼리로 검색할 수 있다.

```sql
SELECT query, frequency
FROM query_frequency
WHERE query LIKE 'tw%'
ORDER BY frequency DESC
LIMIT 5;
```

이 방식은 이해하기 쉽지만, 데이터가 많아질수록 접두어 검색과 정렬 비용이 증가한다.

사용자가 문자를 입력할 때마다 같은 형태의 쿼리가 반복되기 때문에 대규모 자동완성 시스템에는 적합하지 않을 수 있다.

따라서 접두어 검색에 적합한 자료구조가 필요하다.

---

## 7. 트라이 자료구조

트라이는 문자열 검색에 사용하는 트리 형태의 자료구조다.

각 노드는 문자 하나를 나타내며, 루트 노드부터 특정 노드까지의 경로가 하나의 문자열을 나타낸다.

다음 검색어가 있다고 가정한다.

```text
tree
try
true
toy
```

트라이는 다음과 같은 형태로 구성할 수 있다.

```text
root
└─ t
   ├─ r
   │  ├─ e
   │  │  └─ e
   │  ├─ y
   │  └─ u
   │     └─ e
   └─ o
      └─ y
```

`tr`로 시작하는 검색어를 찾으려면 루트에서 `t`, `r` 노드까지 이동한 뒤 그 아래의 모든 검색어를 탐색한다.

```text
tree
try
true
```

트라이는 공통 접두어를 공유하기 때문에 접두어 검색에 적합하다.

---

## 8. 기본 트라이 검색의 시간 복잡도

자동완성 결과를 찾는 과정은 크게 세 단계로 나눌 수 있다.

1. 입력된 접두어의 마지막 노드를 찾는다.
2. 해당 노드 아래에 있는 모든 검색어를 탐색한다.
3. 검색어를 빈도순으로 정렬하고 상위 결과를 반환한다.

입력한 접두어의 길이를 `p`, 해당 노드 아래에 있는 전체 노드 수를 `c`, 반환할 결과 개수를 `k`라고 하면 기본적인 탐색에는 다음과 같은 비용이 발생한다.

```text
접두어 탐색
+ 하위 트라이 탐색
+ 검색 결과 정렬
```

접두어가 짧을수록 하위에 포함된 검색어가 많아진다.

예를 들어 `t`로 검색하면 매우 많은 검색어를 탐색해야 할 수 있다.

따라서 단순히 하위 노드를 모두 탐색하고 매번 정렬하는 방식은 응답 시간이 느려질 수 있다.

---

## 9. 트라이 최적화

### 9.1 접두어 길이 제한

자동완성 시스템에 저장되는 검색어의 최대 길이를 제한할 수 있다.

사용자가 지나치게 긴 문자열을 입력하더라도 일정 길이까지만 자동완성을 지원하면 탐색 범위를 제한할 수 있다.

---

### 9.2 각 노드에 인기 검색어 저장

매 요청마다 하위 트라이 전체를 탐색하지 않도록 각 노드에 해당 접두어로 시작하는 상위 검색어를 미리 저장할 수 있다.

예를 들어 `tr` 노드에 다음 데이터를 저장한다.

```text
tr 노드의 상위 검색어

1. tree
2. try
3. true
```

사용자가 `tr`을 입력하면 하위 노드를 전부 탐색하지 않고 해당 노드에 저장된 결과를 바로 반환한다.

이 경우 검색 과정은 다음과 같이 단순해진다.

1. 접두어에 해당하는 노드를 찾는다.
2. 노드에 저장된 상위 검색어를 반환한다.

조회 성능은 빨라지지만, 각 노드마다 상위 검색어 목록을 저장해야 하므로 메모리 사용량은 증가한다.

즉, 조회 성능을 높이는 대신 더 많은 저장 공간을 사용하는 구조다.

---

## 10. 트라이 갱신

검색어 빈도는 계속 변하기 때문에 트라이에 저장된 검색어 순위도 갱신해야 한다.

갱신 방법은 크게 두 가지로 생각할 수 있다.

### 매주 트라이를 새로 생성하는 방법

일정 기간 동안 검색 로그를 수집한 뒤 검색 빈도를 계산하고, 전체 트라이를 새로 만든다.

```text
검색 로그 수집
→ 검색 빈도 계산
→ 새로운 트라이 생성
→ 기존 트라이 교체
```

구현이 단순하고 트라이의 상태를 일관되게 유지하기 쉽다.

반면 새로운 검색어와 검색량 변화가 즉시 반영되지 않는다.

---

### 트라이의 일부 노드만 갱신하는 방법

검색 빈도가 변경된 검색어와 관련된 노드만 수정할 수도 있다.

예를 들어 `tree`의 검색 빈도가 변경되었다면 다음 노드의 인기 검색어 목록을 갱신해야 한다.

```text
t
tr
tre
tree
```

전체 트라이를 다시 생성하지 않아도 되지만, 변경된 검색어가 영향을 주는 모든 접두어 노드를 수정해야 하므로 구현이 복잡해질 수 있다.

---

## 11. 데이터 수집 서비스와 검색 서비스 분리

자동완성 시스템은 크게 두 부분으로 나눌 수 있다.

### 데이터 수집 서비스

사용자가 검색한 검색어를 수집한다.

```text
사용자 검색
→ 검색 로그 저장
→ 검색어별 빈도 계산
```

### 검색 서비스

사용자가 입력한 접두어를 바탕으로 자동완성 결과를 반환한다.

```text
사용자 입력
→ 접두어 탐색
→ 상위 검색어 반환
```

두 서비스를 분리하면 검색 데이터 집계 작업이 자동완성 API의 응답 속도에 영향을 주는 것을 줄일 수 있다.

---

## 12. 상세 시스템 구성

전체적인 처리 과정은 다음과 같다.

```text
[사용자]
   │
   │ 검색어 입력
   ▼
[로드밸런서]
   │
   ▼
[자동완성 API 서버]
   │
   ├─ 캐시 조회
   │
   └─ 트라이 조회
   │
   ▼
[자동완성 결과 반환]
```

검색 데이터 수집 과정은 다음과 같이 분리된다.

```text
[사용자 검색]
   │
   ▼
[검색 로그 수집]
   │
   ▼
[로그 저장소]
   │
   ▼
[집계 작업]
   │
   ▼
[검색어 빈도 테이블]
   │
   ▼
[트라이 생성 또는 갱신]
```

조회 경로와 데이터 집계 경로를 분리함으로써 읽기 요청을 빠르게 처리할 수 있다.

---

## 13. 캐시

자동완성 요청은 동일한 접두어에 대해 반복해서 발생할 가능성이 높다.

예를 들어 많은 사용자가 `tw`를 입력하면 같은 자동완성 결과가 여러 번 요청된다.

따라서 자주 요청되는 접두어와 검색 결과를 캐시에 저장할 수 있다.

```text
key: tw

value:
- twitter
- twitch
- twitter login
```

캐시를 사용하면 트라이를 직접 조회하는 횟수를 줄이고 응답 속도를 높일 수 있다.

자동완성 결과가 실시간으로 자주 바뀌지 않는다면 캐시 활용 효과가 크다.

---

## 14. 트라이 저장소

트라이 데이터는 메모리에 저장할 수 있다.

메모리에 저장하면 접두어 탐색 속도가 빠르지만, 한 서버의 메모리 용량에는 한계가 있다.

데이터가 많아지면 트라이를 여러 서버에 분산해야 한다.

책에서는 트라이 데이터를 분할하는 기준으로 다음과 같은 방법을 고려한다.

### 검색어 범위로 분할

검색어의 첫 글자를 기준으로 서버를 나눌 수 있다.

```text
A~M 검색어 → 서버 1
N~Z 검색어 → 서버 2
```

하지만 특정 문자로 시작하는 검색어가 많으면 데이터가 한 서버에 집중될 수 있다.

---

### 과거 데이터 분포를 기준으로 분할

각 문자로 시작하는 검색어의 데이터 크기를 분석한 뒤, 서버별 데이터 크기가 비슷하도록 범위를 나눈다.

```text
A~C → 서버 1
D~F → 서버 2
G~K → 서버 3
...
```

단순히 알파벳 개수를 동일하게 나누는 것보다 데이터 분포를 고려한 방식이 더 균등하게 데이터를 분산할 수 있다.

---

## 15. 트라이 복제

자동완성 서비스는 읽기 요청이 매우 많기 때문에 하나의 트라이 서버만 사용하면 장애와 부하에 취약하다.

따라서 같은 트라이 데이터를 여러 서버에 복제할 수 있다.

```text
             ┌─ 트라이 서버 1
자동완성 API ├─ 트라이 서버 2
             └─ 트라이 서버 3
```

복제본을 여러 개 두면 읽기 요청을 분산할 수 있고, 일부 서버에 장애가 발생하더라도 서비스를 계속 제공할 수 있다.

---

## 16. 새로운 검색어 처리

기존 트라이에 없는 새로운 검색어가 갑자기 유행할 수 있다.

하지만 트라이를 일정 주기로만 갱신하면 새로운 검색어가 자동완성 결과에 바로 나타나지 않는다.

책에서는 자동완성 결과가 반드시 실시간일 필요는 없다는 요구사항을 두고, 검색 로그를 일정 주기로 집계해 반영하는 방식을 사용한다.

따라서 이 시스템은 완전한 실시간 시스템이라기보다 일정한 지연을 허용하는 시스템이다.

---

## 17. 유해 검색어 처리

자동완성 결과에는 개인정보, 욕설, 폭력적 표현 등 사용자에게 노출하면 안 되는 검색어가 포함될 수 있다.

이러한 검색어는 자동완성 트라이를 만들기 전에 필터링할 수 있다.

```text
검색 로그
→ 검색어 집계
→ 유해 검색어 필터링
→ 트라이 생성
```

이미 트라이에 포함된 검색어를 긴급하게 제거해야 한다면 별도의 차단 목록을 사용해 자동완성 결과에서 제외할 수도 있다.

---

## 18. 장애 대응

자동완성 시스템에 장애가 발생하더라도 검색 기능 자체는 사용할 수 있어야 한다.

자동완성은 검색을 도와주는 부가 기능이기 때문에, 자동완성 서버가 응답하지 않는 경우 검색창 입력을 막아서는 안 된다.

```text
자동완성 API 정상
→ 추천 검색어 표시

자동완성 API 장애
→ 추천 결과 없이 검색 기능 사용
```

자동완성 요청에는 짧은 타임아웃을 적용하고, 응답이 늦으면 결과를 표시하지 않는 방식으로 처리할 수 있다.

---

## 19. 최종 설계

최종 시스템은 다음과 같이 정리할 수 있다.

```text
                   ┌──────────────────────┐
                   │   데이터 수집 경로    │
                   └──────────────────────┘

사용자 검색
   │
   ▼
검색 로그 수집
   │
   ▼
로그 저장소
   │
   ▼
검색 빈도 집계
   │
   ▼
유해 검색어 필터링
   │
   ▼
트라이 생성 또는 갱신
   │
   ▼
트라이 저장소 및 복제 서버


                   ┌──────────────────────┐
                   │      조회 경로        │
                   └──────────────────────┘

사용자 입력
   │
   ▼
로드밸런서
   │
   ▼
자동완성 API 서버
   │
   ├─ 캐시 조회
   │
   └─ 트라이 조회
   │
   ▼
상위 자동완성 검색어 반환
```

핵심은 데이터 수집과 분석은 비동기적으로 처리하고, 사용자 요청 시에는 미리 만들어진 트라이와 캐시를 이용해 빠르게 응답하는 것이다.